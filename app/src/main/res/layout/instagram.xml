<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
              xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:orientation="vertical"
              android:paddingLeft="10dp"
              android:paddingRight="10dp"
              android:layout_width="match_parent"
              android:layout_height="match_parent">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/iv_instagram"
        android:src="@drawable/random_pic"/>

    <android.support.design.widget.FloatingActionButton
        android:layout_marginBottom="10dp"
        android:id="@+id/fab_instagram_take_pic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:src="@android:drawable/ic_menu_camera"
        android:tint="@android:color/transparent"
        android:clickable="true"
        app:layout_anchor="@id/iv_instagram"
        app:layout_anchorGravity="center|bottom"/>
    <android.support.design.widget.FloatingActionButton
        android:layout_marginBottom="10dp"
        android:layout_marginLeft="10dp"
        android:id="@+id/fab_instagram_rePic"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:src="@android:drawable/ic_menu_camera"
        android:tint="@android:color/transparent"
        android:clickable="true"
        app:layout_anchor="@id/iv_instagram"
        app:layout_anchorGravity="left|bottom"/>
    <android.support.design.widget.FloatingActionButton
        android:layout_marginBottom="10dp"
        android:id="@+id/fab_instagram_instagram"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:src="@drawable/ic_instagram"
        android:tint="@android:color/transparent"
        android:clickable="true"
        app:layout_anchor="@id/iv_instagram"
        app:layout_anchorGravity="center|bottom"/>
    <android.support.design.widget.FloatingActionButton
        android:layout_marginBottom="10dp"
        android:layout_marginRight="10dp"
        android:id="@+id/fab_instagram_share"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:src="@android:drawable/ic_menu_share"
        android:tint="@android:color/transparent"
        android:clickable="true"
        app:layout_anchor="@id/iv_instagram"
        app:layout_anchorGravity="right|bottom"/>
</android.support.design.widget.CoordinatorLayout>